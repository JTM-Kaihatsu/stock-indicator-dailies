{
  "name": "stock-indicator-dailies",
  "version": "0.0.0",
  "private": true,
  "description": "AI-driven technical-analysis utility: ticker in, Buy/Sell/Hold out.",
  "workspaces": [
    "packages/*",
    "apps/*",
    "evals/*"
  ],
  "scripts": {
    "test": "node --test \"packages/**/test/**/*.test.ts\" \"evals/**/test/**/*.test.ts\" \"apps/**/test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit -p packages/shared/tsconfig.json && tsc --noEmit -p packages/indicators/tsconfig.json && tsc --noEmit -p packages/vlm/tsconfig.json && tsc --noEmit -p packages/agent/tsconfig.json && tsc --noEmit -p packages/daily/tsconfig.json && tsc --noEmit -p packages/advisor/tsconfig.json && tsc --noEmit -p evals/interpretation/tsconfig.json && tsc --noEmit -p evals/backtest/tsconfig.json && tsc --noEmit -p apps/api/tsconfig.json && tsc --noEmit -p apps/web/tsconfig.json && tsc --noEmit -p apps/mcp/tsconfig.json",
    "dev:api": "npm run dev -w @stock-indicator-dailies/api",
    "dev:web": "npm run dev -w @stock-indicator-dailies/web"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22"
  }
}
